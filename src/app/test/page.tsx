import { StaggerButton } from "./stagger-button";

const TestPage = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center gap-5">
      <StaggerButton size="lg">
        Contact me
      </StaggerButton>
    </div>
  );
};

export default TestPage;
